services:
  chat_storage:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chat_storage_api
    env_file:
      - .env.example
    ports:
      - "8000:8000"
    restart: unless-stopped